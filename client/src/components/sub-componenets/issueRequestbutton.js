import React from 'react';
import { Button, Icon } from 'semantic-ui-react';

const IssueRequestButton = () => {
  return (
    <Button fluid>Create Issue Request</Button>
  )
};

export default (IssueRequestButton);